

const usersReducer = (state = {}, action) => {
  Object.freeze(state); 

  switch (action.type) {
    // case value:
          
  
    default:
      return state; 
  }
}

export default usersReducer; 